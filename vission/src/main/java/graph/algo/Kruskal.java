package graph.algo;

import graph.pojo.Graph;

/**
 * 最小生成树K*算法
 * 边从小到大排序
 * 没有使用并查集，利用集合，判断是否成环
 */
public class Kruskal {
    public Graph kruskal(Graph graph) {
        Graph result = new Graph();

        return result;
    }
}
